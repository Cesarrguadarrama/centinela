/**
 * Centinela application layer.
 *
 * <p>Use cases that orchestrate the domain: scoring incoming transactions, evaluating configurable
 * rules, raising alerts. Depends only on the domain module; framework adapters live in bootstrap.
 *
 * <p>Populated in phase 2.
 */
package mx.centinela.application;
